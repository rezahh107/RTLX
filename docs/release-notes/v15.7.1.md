# RTLX 15.7.2 — Cache and Injection Correctness

This patch fixes the text-decision cache, normalizes cross-browser programmatic injection evidence, and extracts aggregate runtime diagnostics from the main frame orchestrator.

Highlights:

- corrected the processed-text signature mismatch;
- added structural context to the runtime-local text fingerprint;
- cached unchanged zero-token text decisions;
- added versioned observed-frame injection outcomes;
- normalized callback and Promise `scripting.executeScript()` behavior;
- fail-closed main-frame confirmation;
- added Chrome-total-failure and Firefox-partial-success unit fixtures;
- extracted `RuntimeEvidenceAccumulator` without changing RuntimeSnapshot 1.3.0;
- fixed font-sanitized source tests so they validate the font manifest rather than requiring absent binaries;
- no new permissions, telemetry, remote code, Amazon font files, or user-facing complexity.
