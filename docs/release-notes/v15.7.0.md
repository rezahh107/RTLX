# RTLX 15.7.0 — Precision Direction and Report-Only Diagnosis

Highlights:

- local Vazirmatn/Vazir first, bundled Vazirmatn fallback;
- local Amazon Ember Display/Amazon Ember first, bundled Inter fallback;
- deterministic text-informed semantic-block direction;
- explainable direction action/reason codes;
- block-code versus inline-code distinction;
- Persian natural inline code no longer blanket-forced LTR;
- repair for safe text-only links and buttons while complex controls stay protected;
- wrapper-minimal inline isolation with journal-owned rollback;
- aggregate classification, decision, non-modification, rule-effectiveness, wrapper, and font diagnostics;
- report-only selection of one unfixed area without rule creation or DOM repair;
- no new permissions, telemetry, uploads, remote code, page text, HTML, or Amazon font binaries.

## Verification summary

- 79 test files / 214 tests passed.
- Coverage: statements 85.92%, branches 76.07%, functions 91.03%, lines 87.13%.
- Direct Chromium runtime smoke, schemas, profiles, manifests, security, dependency audits, and four-target builds passed.
- Two release packaging runs and a clean rebuild from the font-sanitized source reproduced all four browser ZIP hashes.
- Exact release-artifact execution remains insufficient in the current environment because Chromium unpacked loading is administrator-blocked and Edge/Firefox/device executables are unavailable.
