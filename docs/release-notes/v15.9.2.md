# RTLX 15.9.2 — Confirmed Runtime Repairs

## Implemented

- handles inherited `dir="auto"` for confident Persian/mixed blocks without overriding explicit local `auto`;
- separates ownership from correctness in not-modified diagnostics;
- recovers connected orphaned text-block continuations through the existing queue;
- cancels abandoned discoveries and registers fallback regions before processing;
- adds required/optional/alternative-group profile-health semantics;
- updates ChatGPT profile health metadata while preserving selectors;
- preserves selected-element evidence inside the same SPA conversation scope;
- blocks known unusable hidden/inactive-tab export from the popup;
- adds focused regression tests for each confirmed issue.

## Deliberately unchanged

- classifier thresholds and lexical rules;
- Qwen, Claude, and ChatGPT selectors;
- streaming queue v2 and degradation policy;
- browser permissions and privacy exclusions;
- automatic screenshot or DOM capture.

## Verification boundary

Repository checks and controlled browser fixtures establish local implementation status. Real-site effectiveness remains `insufficient_evidence` until the packaged 15.9.2 artifact is tested on clean live pages.
