# RTLX 12.0.0 implementation baseline

This release establishes the first repository aligned to the frozen v12 architecture. It is a development baseline, not an accepted store release. See `IMPLEMENTATION_REPORT.md` and `docs/release-checklist.md` for unpassed gates.
