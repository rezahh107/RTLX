# RTLX 15.7.2 — Profile Precision & Diagnostic Clarity

This patch is based on privacy-safe runtime reports captured from Qwen and DeepSeek with RTLX 15.7.1.

Highlights:

- Qwen and DeepSeek protective-only profile v2 without invented DOM selectors;
- safe text-only interactive owner resolution;
- natural-language inline code allowed to follow semantic direction;
- profile-health semantics that do not penalize absent optional protection zones;
- cache hit/miss/store evidence;
- explicit lifetime/current-DOM metric scopes;
- clearer rule-effectiveness names;
- stale selected-element cleanup;
- separate report capture and analysis statuses;
- current-setting-derived font expectation text.

No permission, telemetry, upload, remote-code, page-text, screenshot, or Amazon font redistribution boundary changed.
