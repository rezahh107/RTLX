# راهنمای بستهٔ تحویل RTLX 15.9.4

بستهٔ کامل تحویل شامل سورس کامل، بسته‌های Chromium/Edge/Firefox/Firefox Android، نصب Unpacked، Patch نسخهٔ 15.9.3، گزارش پیاده‌سازی، Manifestهای SHA-256 و Evidence اجرای Gateها است.

وضعیت انتشار:

```text
LOCAL_GATES_PASSED_EXTERNAL_RUNTIME_INSUFFICIENT_EVIDENCE
```

Gateهای محلی و Fixture کنترل‌شده اجرا شده‌اند. اثربخشی نهایی روی Qwen و سایر سایت‌های واقعی فقط پس از نصب نسخهٔ 15.9.4 و ثبت گزارش Runtime جدید قابل تأیید است.
