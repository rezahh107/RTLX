# Store disclosure

RTLX analyzes text only inside the local browser frame. Page text, full URLs, form data, clipboard content, history, screenshots, and DOM snapshots are neither transmitted nor persisted. Persistent site access is optional and requested only after an explicit user action. The extension loads no remote executable code.
