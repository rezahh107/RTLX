# Integration fixtures

`tests/fixtures/01-*` through `25-*` are deterministic synthetic fixture inputs and assertions. They establish repository shape and acceptance scenarios, but they are not a substitute for real browser-extension execution. Runtime execution evidence belongs in the browser E2E report.
