{
  "fixtureVersion": "1.0.0",
  "assertions": [
    "burst mutations are coalesced inside the bounded quiet window",
    "no duplicate bidi wrapper is created",
    "rollback preserves the final text sequence"
  ]
}
