# تحویل RTLX 15.9.8

این نسخه اصلاحات تشخیصی و اعتبارسنجی پیام‌رسانی را اعمال می‌کند و رفتار RTL، پروفایل‌ها، مجوزها و ذخیره‌سازی را تغییر نمی‌دهد.

وضعیت پیاده‌سازی: کامل.

وضعیت انتشار عمومی: تا پاس‌شدن اجرای exact-artifact در Chrome و Firefox Desktop، `NO_GO`.

بسته شامل سورس کامل، چهار خروجی مرورگر، گزارش، patch، هش‌ها و شواهد JSON اجرای مرورگر است.
